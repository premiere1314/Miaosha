server:
  port: 80

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:// 数据库路径 ?useUnicode=true&characterEncoding=utf-8&serverTimezone=Hongkong
    username: 
    password: 
  #jpa
  jpa:
    database: mysql
    #show-sql: true
    hibernate:
      ddl-auto: update
    generate-ddl: true
  security:
    basic:
      path: /swagger-ui.html
      enabled: true
    user:
      name: admin
      password: 1234

